import { describe, it } from "vitest";
import { SortStrategy } from "./SortStrategy";

describe("SortStrategy", () => {
  // ここに it(...) を書いてカバレッジ％を上げよう（見本は TodoItem.test.ts）
  it.todo("priority で高い順に並ぶ");
  it.todo("created で作成日時順に並ぶ");
  it.todo("due で期限順に並ぶ（期限なしは末尾）");
  it.todo("desc=true で逆順になる");
});
